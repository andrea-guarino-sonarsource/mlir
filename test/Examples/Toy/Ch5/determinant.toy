# RUN: toyc-ch3 %s -emit=ast 2>&1 | FileCheck %s


# User defined generic function that operates solely on 
def multiply_determinant(a, b) {
  return determinant(a);
}

def main() {
  var a = [[1, 2, 3], [4, 5, 6]];
  var b<3, 2> = [1, 2, 3, 4, 5, 6];
  var c = multiply_determinant(a, b);
}


# CHECK: Module:
